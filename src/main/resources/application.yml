spring:
  application:
    name: spark-cms
  profiles:
    active: local
server:
  port: 8888
  
graphql:
  extended-scalars: Date
---
spring:
  config:
    activate:
      on-profile: "local"
  data:
    mongodb:
      uri: mongodb://localhost:27017/spark-cms
  

  
